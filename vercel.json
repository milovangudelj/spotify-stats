{
  "builds": [
    {
      "src": "dist/api/index.js",
      "use": "@now/node"
    }
  ],
  "rewrites": [{ "source": "/api/(.*)", "destination": "/api" }],
  "functions": {
    "api/*.ts": {
      "memory": 128,
      "maxDuration": 10
    }
  }
}
